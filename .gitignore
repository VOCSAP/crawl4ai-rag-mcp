.env
.venv
__pycache__
crawl4ai_mcp.egg-info
repos
.claude
test_script_hallucination*

# Graphify output cache
graphify-out/

# HuggingFace reranker cache: never version it (the model is baked into the
# Docker image at build time). A committed cache once caused a git pull to
# delete the host cache and break offline rerank.
hf_cache/

# Planning and session-local files (not part of the public repo)
task_plan.md
findings.md
progress.md
crawl4ai-rag-mcp-plan.md
.planning/
tests/
test_agentic.py
CLAUDE.md
KNOWN_ISSUES.md
.kleos-space
